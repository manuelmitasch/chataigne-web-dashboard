<PasswordForm @dashboard={{ this.settings }} @transition={{ this.previousTransition }} @retryTransition={{ this.previousTransition }}>
        Please enter password to unlock dashboards   
</PasswordForm>

{{outlet}}