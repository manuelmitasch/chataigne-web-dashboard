<ControlContainer 
    @control={{@control}} 
    @showLabel={{ false }}
>
    <div class="clear"></div>
    <button 
        {{on "touchstart" this.update}} 
        {{on "mousedown" this.update}} 
        class="{{ if @control.triggered "triggered" }}"
    >   
        {{#if @control.showLabel }}
            {{@control.label}}
        {{/if}}
    </button>

    {{yield}}
</ControlContainer>