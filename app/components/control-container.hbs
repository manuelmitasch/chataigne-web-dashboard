<div class="control {{ @extraClass }} {{ @control.modelName }}" {{ style this.positionStyles }}>
    {{#if this.showLabel }}
        <label for={{@control.id}} class="control-label unselectable">
            {{@control.label}}
        </label>
    {{/if}}

    {{yield}}
</div>